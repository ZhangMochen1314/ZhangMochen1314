- [x] 后端 API 路由（如 `/api/threads`, `/api/runs`, `/api/agents` 等）必须提供合法的 JWT 才能访问。
- [x] 前端不再使用硬编码的 Mock 用户数据，而是请求真实的 `/auth/me` 获取用户并存储状态。
- [x] 运行大模型（Agent Run）后，会正确计算消耗的 Token 并自动扣减数据库中对应用户的 `credits`。
- [x] 用户 `credits` 为 0 或低于最小阈值时，无法启动新的大模型请求。
- [x] 前端在文件上传时，如果单文件超过 50MB 或者一次选择超过 5 个文件，会抛出提示且阻止上传。
- [x] 后端文件上传接口最大支持限制已被同步为 50MB。
- [x] 后端包含 `cleanup_sandbox.py` 清理脚本，并且有正确的逻辑用于删除 7 天以上的临时文件。