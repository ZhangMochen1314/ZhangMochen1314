# Tasks

- [x] Task 1: 更新 `skills/custom/DeepResValue-Spatial/SKILL.md`，补充 `statspai.spatial.panel.estimator.spatial_panel` 的参数说明（如 `entity`, `time`, `effects`）。
- [x] Task 2: 更新 `skills/custom/DeepResValue-Spatial/SKILL.md`，添加空间诊断（Spatial Diagnostics）部分，明确 `lm_tests` 和 `moran_residuals` 的调用方式。
- [x] Task 3: 更新 `skills/custom/DeepResValue-Spatial/SKILL.md`，添加探索性空间数据分析绘图（ESDA Plots）部分，明确 `moran_plot` 和 `lisa_cluster_map` 的调用及参数（如 `p_threshold`）。
- [x] Task 4: 更新 `skills/custom/DeepResValue-Spatial/SKILL.md`，补充前沿空间模型，如空间双重差分（`spatial_did`）和空间工具变量（`spatial_iv`）。

# Task Dependencies
- 所有任务相互独立，均可并行修改 `SKILL.md` 的不同段落。